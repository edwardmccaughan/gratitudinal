= form_with(model: gratitude, local: true) do |form|
  - if gratitude.errors.any?
    #error_explanation
      %h2
        = pluralize(gratitude.errors.count, "error")
        prohibited this gratitude from being saved:
      %ul
        - gratitude.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :user_id
    = form.number_field :user_id
  .field
    = form.label :body
    = form.text_field :body
  .actions
    = form.submit
